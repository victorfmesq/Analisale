import { FC } from "react";
import { VariantTable } from "../../Table/types";

// entity label é para as labels que usem o nome da entidade

interface CrudProps {
  variant: VariantTable;
}

const Crud: FC<CrudProps> = () => {
  return <></>;
};
